import React from 'react'

export const Skills: React.FC = () => {
    return (
        <section>
            <h2 className="text-xl font-semibold">スキル</h2>
            <p className="text-sm text-zinc-600">ここは skills コンポーネントです — ユーザーのスキル表示／編集 UI を入れます。</p>
        </section>
    )
}

export default Skills
