/**
 * settings ルート用コンポーネント集約
 *
 * このファイルは settings ページで使用するコンポーネントを
 * 名前付きエクスポートで集約します。
 * ページ側では `import * as Settings from '@/components/settings'` として使用します。
 */

export { Settings } from '../pages/auth/Settings/Settings';
