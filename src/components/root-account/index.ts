/**
 * root-account ルート用コンポーネント集約
 *
 * このファイルは root-account ページで使用するコンポーネントを
 * 名前付きエクスポートで集約します。
 * ページ側では `import * as RootAccount from '@/components/root-account'` として使用します。
 */

export { RootAccountsDashboard } from '../root-accounts/root-accounts-dashboard/root-accounts-dashboard';
export { CreateRootAccount } from '../root-accounts/CreateRootAccount/CreateRootAccount';
