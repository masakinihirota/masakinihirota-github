/**
 * onboarding ルート用コンポーネント集約
 *
 * このファイルは onboarding ページで使用するコンポーネントを
 * 名前付きエクスポートで集約します。
 * ページ側では `import * as Onboarding from '@/components/onboarding'` として使用します。
 */

export { CreateRootAccountForm } from './CreateRootAccountForm/CreateRootAccountForm';
