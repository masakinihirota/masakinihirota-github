/**
 * skills ルート用コンポーネント集約
 *
 * このファイルは skills ページで使用するコンポーネントを
 * 名前付きエクスポートで集約します。
 * ページ側では `import * as Skills from '@/components/skills'` として使用します。
 */

export { Skills } from '../pages/auth/Skills';
