# 計画レビューと次の実装ステップ

以下は今回追加したドキュメントと、直ちに着手するための具体的な次のアクションです。

## 追加したドキュメント ✅
- `implementation_plan.md` — フェーズ分割、優先順位、TDD/コロケーション方針
- `mvp_tasks.md` — MVP を小タスクに分割した優先度順リスト
- `tdd_testing_plan.md` — TDD のルールとテストテンプレート

## 直近の優先作業（提案）

1. フェーズ0（環境準備）
   - ローカル Supabase の起動手順 / .env テンプレートを `masakinihirota` リポジトリの README に追記
   - `vitest` のサンプルテストを `masakinihirota` のトップレベルに追加して、テスト実行手順を確立

2. フェーズ1（認証） — 最優先
   - Acceptance Criteria（例: ログインに成功すると /profile にリダイレクトされる）を短く確定する
   - 失敗するテスト（UI とユニット）を `masakinihirota` 側で作成
   - 最小の実装を作りテストを通す。環境変数と秘密情報の扱いに注意

3. 小さなリリースごとにレビューを行い、次のタスクを当面の優先順に更新する

## チェックリスト（認証タスクを始めるとき）

1. `masakinihirota` に `tests/` と `tests/integration/` を用意
2. `Vitest` を使ったサンプルテストを 1 つ用意してパスすることを確認
3. ローカル Supabase の起動手順（ショート README）を準備
4. 認証のテストを作る（失敗するテスト → 実装 → テストが通る）

---

完了: この設計リポジトリに実装プランと実行可能なタスクを追加しました。次は `masakinihirota` 実装リポジトリに移動して、フェーズ0 を開始（テストスキャフォールド + ローカル Supabase ドキュメント整備）することを提案します。
